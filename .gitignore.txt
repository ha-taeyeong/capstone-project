# ========================
# 1. macOS (맥 사용자가 만드는 충돌 파일)
# ========================
# 폴더 아이콘 위치, 배경 설정 등이 담긴 파일 (가장 골칫덩어리)
.DS_Store
.DS_Store?

# 맥의 리소스 포크 파일 (윈도우에서 보면 ._파일이 생기는 원인)
._*
.Spotlight-V100
.Trashes
.AppleDouble
.LSOverride

# ========================
# 2. Windows (윈도우 사용자가 만드는 충돌 파일)
# ========================
# 이미지 미리보기 캐시 파일
Thumbs.db
ehthumbs.db

# 폴더 설정 파일
Desktop.ini

# 바로가기 및 휴지통
*.lnk
$RECYCLE.BIN/

# ========================
# 3. 경로 충돌 방지 (IDE 설정)
# ========================
# 윈도우(C:\)와 맥(/Users/)은 경로 방식이 달라
# 설정 파일을 공유하면 무조건 에러가 납니다.
.idea/
.vscode/
*.swp